# API base URL. When served via the Django backend on the same domain, keep as /api
REACT_APP_API_BASE=/api

# Optional Supabase placeholders (not required unless integrating Supabase auth/storage)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
REACT_APP_SITE_URL=
